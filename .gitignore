node_modules
dist
.DS_Store
.env

# Family reference photos used to prompt PixelLab; keep them out of a public repo.
reference/
# Stray downloads from the PixelLab web app
pixellab-*.png

# Product photo the user supplied as a PixelLab reference; not ours to publish.
art/pixellab/raw/stomp_rocket_photo.png
